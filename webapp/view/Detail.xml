<mvc:View xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m"
  controllerName="appviewcatalog.controller.Detail">
  
  <Page title="{i18n>detailTitle}">
    
    <content>
    <Text  text="Vista Detalle"/>
      <Text text="Detalles del Objeto Maestro" />
      <Text text="{ObjectName}" />
      <Text text="{Status}" />
      <Text text="{ObjectCount}" />
      <Text text="{LastUpdate}" />
    </content>
  </Page>
  
</mvc:View>